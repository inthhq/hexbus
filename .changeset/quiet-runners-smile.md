---
"hexbus": minor
---

Add `runCli`, a shared CLI lifecycle runner that composes Hexbus primitives for version output, context creation, update checks, help, intro rendering, command dispatch, hooks, telemetry shutdown, and error handling.
