---
"hexbus": minor
---

Add `dispatchCommand`, `selectCommand`, and `findCommand` helpers for reusable command lookup, unknown-command handling, no-command behavior, and interactive command selection.
