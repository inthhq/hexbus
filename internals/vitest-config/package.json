{
	"name": "@inth/vitest-config",
	"version": "0.1.0",
	"type": "module",
	"exports": {
		"./base": {
			"types": "./src/base-config.ts",
			"import": "./src/base-config.ts"
		},
		"./ui": {
			"types": "./src/ui-config.ts",
			"import": "./src/ui-config.ts"
		}
	},
	"private": true,
	"scripts": {
		"check-types": "tsc --noEmit",
		"dev": "echo '@inth/vitest-config has no watch task'",
		"lint": "bun biome lint ./src",
		"fmt": "bun biome format --write . && bun biome check --formatter-enabled=false --linter-enabled=false --write",
		"prepublishOnly": "node -e \"throw new Error('@inth/vitest-config is internal and must not be published')\""
	},
	"dependencies": {
		"@vitejs/plugin-react": "6.0.1",
		"vitest": "4.1.2"
	},
	"devDependencies": {
		"@inth/typescript-config": "workspace:*",
		"@vitest/ui": "4.1.2",
		"jsdom": "^29.0.1"
	}
}
