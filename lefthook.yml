pre-commit:
  jobs:
    - run: |
        files="$(git diff --name-only --staged --diff-filter=ACMR | rg '\.(js|jsx|ts|tsx|json|jsonc|css)$' || true)"
        [ -z "$files" ] || bun x ultracite fix $files
      glob:
        - "**/*.js"
        - "**/*.jsx"
        - "**/*.ts"
        - "**/*.tsx"
        - "**/*.json"
        - "**/*.jsonc"
        - "**/*.css"
      stage_fixed: true
